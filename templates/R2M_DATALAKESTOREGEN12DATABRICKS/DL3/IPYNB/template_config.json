{
    "scripts": [
        {
            "script_name": "notebook",
            "output_file": {
                "extension": "ipynb", 
                "sub_folder": "ipynb/{{target.database}}/Master",           
                "name": "{{template_config.properties.dam_prefix}}_{{template_config.properties.pattern}}_{{source.database}}_{{source.schema}}_{{source_tablename_formatted}}"
            }
        }
    ],
    "linked_templates": [
        "R2M_DATALAKESTOREGEN12DATABRICKS/ORCHESTRATION/DATAFACTORY"
    ],
    "properties": {
        "dam_prefix": "R2M",
        "pattern": "DL3"
    },
    "variables": {
        "source_tablename_formatted": "{{#string_func.replace_illegal_characters}}{{source_tablename_formatted}}{{/string_func.replace_illegal_characters}}"
    }
}