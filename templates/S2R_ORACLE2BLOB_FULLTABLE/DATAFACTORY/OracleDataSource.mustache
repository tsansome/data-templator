{
    "name": "Oracle_{{ source.database }}_{{ source.schema }}_{{ source.tablename }}",
    "properties": {
        "linkedServiceName": {
            "referenceName": "{{ source.service_name }}",
            "type": "linkedServiceReference"
        },
        "folder": {
            "name": "{{ pattern }}_{{ source.system_name }}"
        },
        "type": "OracleTable",
        "typeProperties": {
            "format": {
                "tableName": "{{ source.tablename }}"
            }
        }
    }
}